name: Data Pipeline Frontend Worker Release

on:
  push:
    tags:
    - "datapipeline_frontendworker-0.*.*"
    - "datapipeline_frontendworker-0.*.*-alpha*"
    - "datapipeline_frontendworker-0.*.*-beta*"
    - "datapipeline_frontendworker-0.*.*-rc*"
    - "datapipeline_frontendworker-0.*.*-post*"
    - "datapipeline_frontendworker-1.*.*"
    - "datapipeline_frontendworker-1.*.*-alpha*"
    - "datapipeline_frontendworker-1.*.*-beta*"
    - "datapipeline_frontendworker-1.*.*-rc*"
    - "datapipeline_frontendworker-1.*.*-post*"
    - "datapipeline_frontendworker-2.*.*"
    - "datapipeline_frontendworker-2.*.*-alpha*"
    - "datapipeline_frontendworker-2.*.*-beta*"
    - "datapipeline_frontendworker-2.*.*-rc*"
    - "datapipeline_frontendworker-2.*.*-post*"
  workflow_dispatch:
    inputs: {}

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      docker_tag: ${{ steps.docker-tag.outputs.docker_tag }}
    steps:
    - name: Set Docker Tag
      id: docker-tag
      run: |
        DOCKER_TAG="${GITHUB_REF_NAME#datapipeline_frontendworker-}"
        echo "DOCKER_TAG=$DOCKER_TAG" >> $GITHUB_ENV
        echo "docker_tag: Latest Tag is : $DOCKER_TAG"
        echo "docker_tag=$DOCKER_TAG" >> $GITHUB_OUTPUT
  docker_build:
    name: Build Docker Image
    needs:
      - prepare
    uses: ./.github/workflows/build-docker-image.yml
    with:
      image: datapipeline-frontendworker
      context: ./src
      dockerfile: ./src/dotnet/DataPipelineFrontendWorker/Dockerfile
      registry: foundationallm.azurecr.io
      tag: ${{ needs.prepare.outputs.docker_tag }}
      username: foundationallm
      target: sandbox
    secrets: inherit
