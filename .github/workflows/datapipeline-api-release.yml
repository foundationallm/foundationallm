name: Data Pipeline API Release

on:
  push:
    tags:
    - "datapipeline_api-0.*.*"
    - "datapipeline_api-0.*.*-alpha*"
    - "datapipeline_api-0.*.*-beta*"
    - "datapipeline_api-0.*.*-rc*"
    - "datapipeline_api-0.*.*-post*"
    - "datapipeline_api-1.*.*"
    - "datapipeline_api-1.*.*-alpha*"
    - "datapipeline_api-1.*.*-beta*"
    - "datapipeline_api-1.*.*-rc*"
    - "datapipeline_api-1.*.*-post*"
    - "datapipeline_api-2.*.*"
    - "datapipeline_api-2.*.*-alpha*"
    - "datapipeline_api-2.*.*-beta*"
    - "datapipeline_api-2.*.*-rc*"
    - "datapipeline_api-2.*.*-post*"
  workflow_dispatch:
    inputs: {}

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      docker_tag: ${{ steps.docker-tag.outputs.docker_tag }}
    steps:
    - name: Set Docker Tag
      id: docker-tag
      run: |
        DOCKER_TAG="${GITHUB_REF_NAME#datapipeline_api-}"
        echo "DOCKER_TAG=$DOCKER_TAG" >> $GITHUB_ENV
        echo "docker_tag: Latest Tag is : $DOCKER_TAG"
        echo "docker_tag=$DOCKER_TAG" >> $GITHUB_OUTPUT
  docker_build:
    name: Build Docker Image
    needs:
      - prepare
    uses: ./.github/workflows/build-docker-image.yml
    with:
      image: datapipeline-api
      context: ./src
      dockerfile: ./src/dotnet/DataPipelineAPI/Dockerfile
      registry: foundationallm.azurecr.io
      tag: ${{ needs.prepare.outputs.docker_tag }}
      username: foundationallm
      target: sandbox
    secrets: inherit

