name: Management API Release

on:
  push:
    tags:
    - "management_api-0.*.*"
    - "management_api-0.*.*-alpha*"
    - "management_api-0.*.*-beta*"
    - "management_api-0.*.*-rc*"
    - "management_api-0.*.*-post*"
    - "management_api-1.*.*"
    - "management_api-1.*.*-alpha*"
    - "management_api-1.*.*-beta*"
    - "management_api-1.*.*-rc*"
    - "management_api-1.*.*-post*"
    - "management_api-2.*.*"
    - "management_api-2.*.*-alpha*"
    - "management_api-2.*.*-beta*"
    - "management_api-2.*.*-rc*"
    - "management_api-2.*.*-post*"
  workflow_dispatch:
    inputs: {}

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      docker_tag: ${{ steps.docker-tag.outputs.docker_tag }}
    steps:
    - name: Set Docker Tag
      id: docker-tag
      run: |
        DOCKER_TAG="${GITHUB_REF_NAME#core_api-}"
        echo "DOCKER_TAG=$DOCKER_TAG" >> $GITHUB_ENV
        echo "docker_tag: Latest Tag is : $DOCKER_TAG"
        echo "docker_tag=$DOCKER_TAG" >> $GITHUB_OUTPUT
  docker_build:
    name: Build Docker Image
    needs:
      - prepare
    uses: ./.github/workflows/build-docker-image.yml
    with:
      image: management-api
      context: ./src
      dockerfile: ./src/dotnet/ManagementAPI/Dockerfile
      registry: foundationallm.azurecr.io
      tag: ${{ needs.prepare.outputs.docker_tag }}
      username: foundationallm
      target: sandbox
    secrets: inherit
