name: Core API Release

on:
  push:
    tags:
    - "core_api-0.*.*"
    - "core_api-0.*.*-alpha*"
    - "core_api-0.*.*-beta*"
    - "core_api-0.*.*-rc*"
    - "core_api-1.*.*"
    - "core_api-1.*.*-alpha*"
    - "core_api-1.*.*-beta*"
    - "core_api-1.*.*-rc*"
    - "core_api-2.*.*"
    - "core_api-2.*.*-alpha*"
    - "core_api-2.*.*-beta*"
    - "core_api-2.*.*-rc*"
  workflow_dispatch:
    inputs: {}

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      docker_tag: ${{ steps.docker-tag.outputs.docker_tag }}
    steps:
    - name: Set Docker Tag
      id: docker-tag
      run: |
        DOCKER_TAG="${GITHUB_REF_NAME#core_api-}"
        echo "DOCKER_TAG=$DOCKER_TAG" >> $GITHUB_ENV
        echo "docker_tag: Latest Tag is : $DOCKER_TAG"
        echo "docker_tag=$DOCKER_TAG" >> $GITHUB_OUTPUT
  docker_build:
    name: Build Docker Image
    needs:
      - prepare
    uses: ./.github/workflows/build-docker-image.yml
    with:
      image: core-api
      context: ./src
      dockerfile: ./src/dotnet/CoreAPI/Dockerfile
      registry: foundationallm.azurecr.io
      tag: ${{ needs.prepare.outputs.docker_tag }}
      username: foundationallm
      target: sandbox
    secrets: inherit
